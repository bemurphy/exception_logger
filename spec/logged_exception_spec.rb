require File.dirname(__FILE__) + '/spec_helper'

describe LoggedException do
  # it "should have the expected dedupe hash stored" do
  #   @logged_exception.dedupe_hash.should == @expected_dedupe_hash
  # end
end